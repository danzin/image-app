# ===========================================
# DEFAULT STARTING PORTS
# The orchestrator (npm run dev) will auto-find free ports
# starting from these values if they're in use
# ===========================================
BACKEND_PORT=3000
GATEWAY_PORT=8000
FRONTEND_PORT=5173

# Gateway URL (frontend needs this to proxy requests)
GATEWAY_URL=http://localhost:8000
